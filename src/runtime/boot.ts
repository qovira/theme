/**
 * Pre-paint boot snippet — placeholder.
 *
 * Will hold the synchronous, no-flash boot string: resolve `localStorage` →
 * `prefers-color-scheme` → `evening`, then set `data-theme` on `<html>`. This
 * stub fixes the export shape so the build can wire `@qovira/theme/boot` and
 * consumers can import it today.
 */
export const boot = "";
