---
"@qovira/theme": patch
---

Reformat the shipped `theme.css` with the standardized Prettier config
(`printWidth: 180`). No token, utility, runtime, or API changes — the output is
semantically identical; only source formatting differs.
